function myFunction(){
    var np = document.getElementById("new_password").value;
    var rp = document.getElementById("re_password").value;
    if(rp!=np)
    {
        window.alert("不一致");
    }
    else;
}